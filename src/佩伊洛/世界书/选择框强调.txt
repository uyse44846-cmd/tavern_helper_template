---
选择框强调:
  rule: The following must be inserted to the end of each reply and cannot be omitted
  format: |-
    <%= getvar('stat_data.世界.下一回合界面选择') === 'galgame' ? '<roleplay_options>' : '<sprite_choices>' %>
    ```
    ...<%= getvar('stat_data.世界.下一回合界面选择') === 'galgame' ? '$(最多3个选项，每个选项内容必须是主视角的话或行动，不多于20字)' : '' %>
    ```
    <%= getvar('stat_data.世界.下一回合界面选择') === 'galgame' ? '</roleplay_options>' : '</sprite_choices>' %>
